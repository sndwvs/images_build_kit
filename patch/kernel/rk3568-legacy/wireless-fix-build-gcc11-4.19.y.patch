diff -Naur a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/efuse/rtw_efuse.c b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/efuse/rtw_efuse.c
--- a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/efuse/rtw_efuse.c	2021-09-21 17:36:33.310916696 +0000
+++ b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/efuse/rtw_efuse.c	2021-10-08 21:20:05.599131665 +0000
@@ -898,8 +898,9 @@
 	j = 0;
 
 	for (i = 0; i < mapLen; i++) {
-		if (i % 16 == 0)
+		if (i % 16 == 0) {
 			RTW_PRINT_SEL(RTW_DBGDUMP, "0x%03x: ", i);
+		}
 			_RTW_PRINT_SEL(RTW_DBGDUMP, "%02X%s"
 				, pEfuseHal->fakeEfuseInitMap[i]
 				, ((i + 1) % 16 == 0) ? "\n" : (((i + 1) % 8 == 0) ? "	  " : " ")
diff -Naur a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_mlme.c b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_mlme.c
--- a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_mlme.c	2021-09-21 17:36:33.350916604 +0000
+++ b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_mlme.c	2021-10-08 20:45:52.751078722 +0000
@@ -3134,12 +3134,14 @@
 	ssc_chk = rtw_sitesurvey_condition_check(padapter, _FALSE);
 	if( ssc_chk == SS_DENY_BUSY_TRAFFIC) {
 		#ifdef CONFIG_LAYER2_ROAMING
-		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE)
+		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE) {
 			RTW_INFO(FUNC_ADPT_FMT" need to roam, don't care BusyTraffic\n", FUNC_ADPT_ARG(padapter));
-		else
+		} else
 		#endif
+		{
 			RTW_INFO(FUNC_ADPT_FMT" exit BusyTraffic\n", FUNC_ADPT_ARG(padapter));
 			goto exit;
+		}
 	}
 	else if (ssc_chk != SS_ALLOW)
 		goto exit;
diff -Naur a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_recv.c b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_recv.c
--- a/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_recv.c	2021-09-21 17:36:33.378916540 +0000
+++ b/drivers/net/wireless/rockchip_wlan/rtl8822ce/core/rtw_recv.c	2021-10-08 21:06:53.364735113 +0000
@@ -3555,9 +3555,10 @@
 			int i;
 			RTW_INFO("############ type:0x%02x subtype:0x%02x #################\n", type, subtype);
 
-			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8)
+			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8) {
 				RTW_INFO("%02X:%02X:%02X:%02X:%02X:%02X:%02X:%02X:\n", *(ptr + i),
 					*(ptr + i + 1), *(ptr + i + 2) , *(ptr + i + 3) , *(ptr + i + 4), *(ptr + i + 5), *(ptr + i + 6), *(ptr + i + 7));
+			}
 				RTW_INFO("#############################\n");
 				_rtw_memset(pmppriv->mplink_buf, '\0' , sizeof(pmppriv->mplink_buf));
 				_rtw_memcpy(pmppriv->mplink_buf, ptr, precv_frame->u.hdr.len);
diff -Naur a/drivers/net/wireless/rtl8192eu/core/efuse/rtw_efuse.c b/drivers/net/wireless/rtl8192eu/core/efuse/rtw_efuse.c
--- a/drivers/net/wireless/rtl8192eu/core/efuse/rtw_efuse.c	2021-10-08 18:41:08.896172313 +0000
+++ b/drivers/net/wireless/rtl8192eu/core/efuse/rtw_efuse.c	2021-10-08 21:20:52.603036416 +0000
@@ -778,8 +778,9 @@
 	j = 0;
 
 	for (i = 0; i < mapLen; i++) {
-		if (i % 16 == 0)
+		if (i % 16 == 0) {
 			RTW_PRINT_SEL(RTW_DBGDUMP, "0x%03x: ", i);
+		}
 			_RTW_PRINT_SEL(RTW_DBGDUMP, "%02X%s"
 				, pEfuseHal->fakeEfuseInitMap[i]
 				, ((i + 1) % 16 == 0) ? "\n" : (((i + 1) % 8 == 0) ? "	  " : " ")
diff -Naur a/drivers/net/wireless/rtl8192eu/core/rtw_mlme.c b/drivers/net/wireless/rtl8192eu/core/rtw_mlme.c
--- a/drivers/net/wireless/rtl8192eu/core/rtw_mlme.c	2021-10-08 18:41:08.948172197 +0000
+++ b/drivers/net/wireless/rtl8192eu/core/rtw_mlme.c	2021-10-08 20:46:23.335022648 +0000
@@ -3191,12 +3191,14 @@
 	ssc_chk = rtw_sitesurvey_condition_check(padapter, _FALSE);
 	if( ssc_chk == SS_DENY_BUSY_TRAFFIC) {
 		#ifdef CONFIG_LAYER2_ROAMING
-		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE)
+		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE) {
 			RTW_INFO(FUNC_ADPT_FMT" need to roam, don't care BusyTraffic\n", FUNC_ADPT_ARG(padapter));
-		else
+		} else
 		#endif
+		{
 			RTW_INFO(FUNC_ADPT_FMT" exit BusyTraffic\n", FUNC_ADPT_ARG(padapter));
 			goto exit;
+		}
 	}
 	else if (ssc_chk != SS_ALLOW)
 		goto exit;
diff -Naur a/drivers/net/wireless/rtl8192eu/core/rtw_wlan_util.c b/drivers/net/wireless/rtl8192eu/core/rtw_wlan_util.c
--- a/drivers/net/wireless/rtl8192eu/core/rtw_wlan_util.c	2021-10-08 18:41:08.992172099 +0000
+++ b/drivers/net/wireless/rtl8192eu/core/rtw_wlan_util.c	2021-10-08 21:04:29.997024860 +0000
@@ -3224,8 +3224,9 @@
 		goto exit;
 	else {
 		if(*(p+1) > 6 ) {
-			for(i=0; i<9;i++)
+			for( i=0; i<9; i++ ) {
 				RTW_INFO("p[%d]=0x%x",i,*(p+i) );
+			}
 				RTW_INFO("\n");
 			if(*(p+6) != 2)
 				goto exit;
diff -Naur a/drivers/net/wireless/rtl8812au/core/efuse/rtw_efuse.c b/drivers/net/wireless/rtl8812au/core/efuse/rtw_efuse.c
--- a/drivers/net/wireless/rtl8812au/core/efuse/rtw_efuse.c	2021-10-08 18:41:11.624166231 +0000
+++ b/drivers/net/wireless/rtl8812au/core/efuse/rtw_efuse.c	2021-10-08 21:21:28.422963823 +0000
@@ -907,8 +907,9 @@
 	j = 0;
 
 	for (i = 0; i < mapLen; i++) {
-		if (i % 16 == 0)
+		if (i % 16 == 0) {
 			RTW_PRINT_SEL(RTW_DBGDUMP, "0x%03x: ", i);
+		}
 			_RTW_PRINT_SEL(RTW_DBGDUMP, "%02X%s"
 				, pEfuseHal->fakeEfuseInitMap[i]
 				, ((i + 1) % 16 == 0) ? "\n" : (((i + 1) % 8 == 0) ? "	  " : " ")
diff -Naur a/drivers/net/wireless/rtl8812au/core/rtw_mlme.c b/drivers/net/wireless/rtl8812au/core/rtw_mlme.c
--- a/drivers/net/wireless/rtl8812au/core/rtw_mlme.c	2021-10-08 18:41:11.676166115 +0000
+++ b/drivers/net/wireless/rtl8812au/core/rtw_mlme.c	2021-10-08 20:46:49.806974892 +0000
@@ -3152,12 +3152,14 @@
 	ssc_chk = rtw_sitesurvey_condition_check(padapter, _FALSE);
 	if( ssc_chk == SS_DENY_BUSY_TRAFFIC) {
 		#ifdef CONFIG_LAYER2_ROAMING
-		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE)
+		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE) {
 			RTW_INFO(FUNC_ADPT_FMT" need to roam, don't care BusyTraffic\n", FUNC_ADPT_ARG(padapter));
-		else
+		} else
 		#endif
+		{
 			RTW_INFO(FUNC_ADPT_FMT" exit BusyTraffic\n", FUNC_ADPT_ARG(padapter));
 			goto exit;
+		}
 	}
 	else if (ssc_chk != SS_ALLOW)
 		goto exit;
diff -Naur a/drivers/net/wireless/rtl8812au/core/rtw_recv.c b/drivers/net/wireless/rtl8812au/core/rtw_recv.c
--- a/drivers/net/wireless/rtl8812au/core/rtw_recv.c	2021-10-08 18:41:11.708166044 +0000
+++ b/drivers/net/wireless/rtl8812au/core/rtw_recv.c	2021-10-08 21:11:42.340150682 +0000
@@ -3595,9 +3595,10 @@
 			int i;
 			RTW_INFO("############ type:0x%02x subtype:0x%02x #################\n", type, subtype);
 
-			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8)
+			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8) {
 				RTW_INFO("%02X:%02X:%02X:%02X:%02X:%02X:%02X:%02X:\n", *(ptr + i),
 					*(ptr + i + 1), *(ptr + i + 2) , *(ptr + i + 3) , *(ptr + i + 4), *(ptr + i + 5), *(ptr + i + 6), *(ptr + i + 7));
+			}
 				RTW_INFO("#############################\n");
 				_rtw_memset(pmppriv->mplink_buf, '\0' , sizeof(pmppriv->mplink_buf));
 				_rtw_memcpy(pmppriv->mplink_buf, ptr, precv_frame->u.hdr.len);
diff -Naur a/drivers/net/wireless/rtl8814au/core/efuse/rtw_efuse.c b/drivers/net/wireless/rtl8814au/core/efuse/rtw_efuse.c
--- a/drivers/net/wireless/rtl8814au/core/efuse/rtw_efuse.c	2021-10-08 18:41:30.664123805 +0000
+++ b/drivers/net/wireless/rtl8814au/core/efuse/rtw_efuse.c	2021-10-08 21:19:04.959254528 +0000
@@ -902,8 +902,9 @@
 	j = 0;
 
 	for (i = 0; i < mapLen; i++) {
-		if (i % 16 == 0)
+		if (i % 16 == 0) {
 			RTW_PRINT_SEL(RTW_DBGDUMP, "0x%03x: ", i);
+		}
 			_RTW_PRINT_SEL(RTW_DBGDUMP, "%02X%s"
 				, pEfuseHal->fakeEfuseInitMap[i]
 				, ((i + 1) % 16 == 0) ? "\n" : (((i + 1) % 8 == 0) ? "	  " : " ")
diff -Naur a/drivers/net/wireless/rtl8814au/core/rtw_mlme.c b/drivers/net/wireless/rtl8814au/core/rtw_mlme.c
--- a/drivers/net/wireless/rtl8814au/core/rtw_mlme.c	2021-10-08 18:41:30.708123707 +0000
+++ b/drivers/net/wireless/rtl8814au/core/rtw_mlme.c	2021-10-08 20:47:04.642948109 +0000
@@ -3125,12 +3125,14 @@
 	ssc_chk = rtw_sitesurvey_condition_check(padapter, _FALSE);
 	if( ssc_chk == SS_DENY_BUSY_TRAFFIC) {
 		#ifdef CONFIG_LAYER2_ROAMING
-		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE)
+		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE) {
 			RTW_INFO(FUNC_ADPT_FMT" need to roam, don't care BusyTraffic\n", FUNC_ADPT_ARG(padapter));
-		else
+		} else
 		#endif
+		{
 			RTW_INFO(FUNC_ADPT_FMT" exit BusyTraffic\n", FUNC_ADPT_ARG(padapter));
 			goto exit;
+		}
 	}
 	else if (ssc_chk != SS_ALLOW)
 		goto exit;
diff -Naur a/drivers/net/wireless/rtl8814au/core/rtw_recv.c b/drivers/net/wireless/rtl8814au/core/rtw_recv.c
--- a/drivers/net/wireless/rtl8814au/core/rtw_recv.c	2021-10-08 18:41:30.736123645 +0000
+++ b/drivers/net/wireless/rtl8814au/core/rtw_recv.c	2021-10-08 21:03:51.037103091 +0000
@@ -3572,9 +3572,10 @@
 			int i;
 			RTW_INFO("############ type:0x%02x subtype:0x%02x #################\n", type, subtype);
 
-			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8)
+			for (i = 0; i < precv_frame->u.hdr.len; i = i + 8) {
 				RTW_INFO("%02X:%02X:%02X:%02X:%02X:%02X:%02X:%02X:\n", *(ptr + i),
 					*(ptr + i + 1), *(ptr + i + 2) , *(ptr + i + 3) , *(ptr + i + 4), *(ptr + i + 5), *(ptr + i + 6), *(ptr + i + 7));
+			}
 				RTW_INFO("#############################\n");
 				_rtw_memset(pmppriv->mplink_buf, '\0' , sizeof(pmppriv->mplink_buf));
 				_rtw_memcpy(pmppriv->mplink_buf, ptr, precv_frame->u.hdr.len);
diff -Naur a/drivers/net/wireless/rtl8822bu/core/efuse/rtw_efuse.c b/drivers/net/wireless/rtl8822bu/core/efuse/rtw_efuse.c
--- a/drivers/net/wireless/rtl8822bu/core/efuse/rtw_efuse.c	2021-10-08 18:41:26.992131984 +0000
+++ b/drivers/net/wireless/rtl8822bu/core/efuse/rtw_efuse.c	2021-10-08 21:30:16.625812359 +0000
@@ -778,8 +778,9 @@
 	j = 0;
 
 	for (i = 0; i < mapLen; i++) {
-		if (i % 16 == 0)
+		if (i % 16 == 0) {
 			RTW_PRINT_SEL(RTW_DBGDUMP, "0x%03x: ", i);
+		}
 			_RTW_PRINT_SEL(RTW_DBGDUMP, "%02X%s"
 				, pEfuseHal->fakeEfuseInitMap[i]
 				, ((i + 1) % 16 == 0) ? "\n" : (((i + 1) % 8 == 0) ? "	  " : " ")
diff -Naur a/drivers/net/wireless/rtl8822bu/core/rtw_mlme.c b/drivers/net/wireless/rtl8822bu/core/rtw_mlme.c
--- a/drivers/net/wireless/rtl8822bu/core/rtw_mlme.c	2021-10-08 18:41:27.040131877 +0000
+++ b/drivers/net/wireless/rtl8822bu/core/rtw_mlme.c	2021-10-08 20:47:18.642922824 +0000
@@ -3191,12 +3191,14 @@
 	ssc_chk = rtw_sitesurvey_condition_check(padapter, _FALSE);
 	if( ssc_chk == SS_DENY_BUSY_TRAFFIC) {
 		#ifdef CONFIG_LAYER2_ROAMING
-		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE)
+		if (rtw_chk_roam_flags(padapter, RTW_ROAM_ACTIVE) && pmlmepriv->need_to_roam == _TRUE) {
 			RTW_INFO(FUNC_ADPT_FMT" need to roam, don't care BusyTraffic\n", FUNC_ADPT_ARG(padapter));
-		else
+		} else
 		#endif
+		{
 			RTW_INFO(FUNC_ADPT_FMT" exit BusyTraffic\n", FUNC_ADPT_ARG(padapter));
 			goto exit;
+		}
 	}
 	else if (ssc_chk != SS_ALLOW)
 		goto exit;
diff -Naur a/drivers/net/wireless/rtl8822bu/core/rtw_wlan_util.c b/drivers/net/wireless/rtl8822bu/core/rtw_wlan_util.c
--- a/drivers/net/wireless/rtl8822bu/core/rtw_wlan_util.c	2021-10-08 18:41:27.084131779 +0000
+++ b/drivers/net/wireless/rtl8822bu/core/rtw_wlan_util.c	2021-10-08 21:04:10.433064388 +0000
@@ -3224,8 +3224,9 @@
 		goto exit;
 	else {
 		if(*(p+1) > 6 ) {
-			for(i=0; i<9;i++)
+			for( i=0; i<9; i++ ) {
 				RTW_INFO("p[%d]=0x%x",i,*(p+i) );
+			}
 				RTW_INFO("\n");
 			if(*(p+6) != 2)
 				goto exit;
