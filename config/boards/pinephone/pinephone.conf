#---------------------------------------------
# pinephone
# A64 quad core 2/3GB RAM SoC WiFi/BT Modem GPS
#---------------------------------------------

BOARD_NAME="pinephone"
SOCFAMILY=sun50iw1
BOOT_LOADER_CONFIG="${BOARD_NAME}_defconfig"
KERNEL_SOURCES=":next"
DESKTOP=yes
MODULES_BLACKLIST="8723cs_old"

case $KERNEL_SOURCE in
    next)
            DEVICE_TREE_BLOB=allwinner/${SOCFAMILY%w*}-a64-${BOARD_NAME}-1.2.dtb
            LINUX_CONFIG="linux-${SOCFAMILY}-${KERNEL_SOURCE}.config"
            ROOT_DISK="mmcblk0p1"
    ;;
esac
