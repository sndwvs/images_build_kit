--- a/drivers/net/wireless/rtl8192eu/os_dep/osdep_service.c	2022-02-08 20:38:26.601807883 +0200
+++ b/drivers/net/wireless/rtl8192eu/os_dep/osdep_service.c	2022-02-08 20:39:43.809989444 +0200
@@ -875,8 +875,12 @@
 inline void thread_exit(_completion *comp)
 {
 #ifdef PLATFORM_LINUX
+#if (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
+	kthread_complete_and_exit(comp, 0);
+#else
 	complete_and_exit(comp, 0);
 #endif
+#endif
 
 }
 
--- a/drivers/net/wireless/rtl8192eu/os_dep/linux/rtw_proc.c	2022-02-08 20:42:20.770358610 +0200
+++ b/drivers/net/wireless/rtl8192eu/os_dep/linux/rtw_proc.c	2022-02-08 20:42:54.026436837 +0200
@@ -37,6 +37,8 @@
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(3, 10, 0))
 #define PDE_DATA(inode) PDE((inode))->data
 #define proc_get_parent_data(inode) PDE((inode))->parent->data
+#elif (LINUX_VERSION_CODE >= KERNEL_VERSION(5, 17, 0))
+#define PDE_DATA(inode) pde_data(inode)
 #endif
 
 #if (LINUX_VERSION_CODE < KERNEL_VERSION(2, 6, 24))
